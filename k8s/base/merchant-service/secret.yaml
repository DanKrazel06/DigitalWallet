# Secret — DATABASE_URL du merchant-service (base "merchant", hote DNS postgres).
apiVersion: v1
kind: Secret
metadata:
  name: merchant-service-secret
type: Opaque
stringData:
  DATABASE_URL: 'postgresql://walletdigital:changeme_pg@postgres:5432/merchant?schema=public'
