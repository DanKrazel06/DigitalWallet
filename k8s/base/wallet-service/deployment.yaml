# Deployment — wallet-service (stateless, cf. tx-service pour les details).
apiVersion: apps/v1
kind: Deployment
metadata:
  name: wallet-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: wallet-service
  template:
    metadata:
      labels:
        app: wallet-service
    spec:
      terminationGracePeriodSeconds: 30
      containers:
        - name: wallet-service
          image: walletdigital/wallet-service:dev
          imagePullPolicy: Never
          ports:
            - name: http
              containerPort: 3003
          envFrom:
            - configMapRef:
                name: wallet-service-config
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: wallet-service-secret
                  key: DATABASE_URL
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 20
            periodSeconds: 15
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: '1'
              memory: 512Mi
