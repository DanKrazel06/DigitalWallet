# Secret — DATABASE_URL du wallet-service (base "wallet", hote DNS postgres).
apiVersion: v1
kind: Secret
metadata:
  name: wallet-service-secret
type: Opaque
stringData:
  DATABASE_URL: 'postgresql://walletdigital:changeme_pg@postgres:5432/wallet?schema=public'
