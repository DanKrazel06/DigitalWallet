# Service (ClusterIP) — wallet-service. DNS interne : wallet-service:3003.
apiVersion: v1
kind: Service
metadata:
  name: wallet-service
spec:
  type: ClusterIP
  selector:
    app: wallet-service
  ports:
    - name: http
      port: 3003
      targetPort: http
