# wallet-service — local environment template.
HOST=0.0.0.0
PORT=3003

NODE_ENV=development
LOG_LEVEL=info

DATABASE_URL=postgresql://walletdigital:changeme_pg@127.0.0.1:5433/wallet?schema=public

KAFKA_BROKERS=localhost:9092
KAFKA_CLIENT_ID=wallet-service
KAFKA_CONSUMER_GROUP=wallet-service

DEFAULT_CURRENCY=USD
